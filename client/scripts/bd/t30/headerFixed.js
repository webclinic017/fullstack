/*控制头部滚动样式*/window.addEventListener("load", function () {    var header = document.querySelector(".t30_header");    var alpha = 1;    header.style.background = "rgba(0,0,0," + alpha + ")";    window.document.addEventListener("scroll", function () {        if (document.body.scrollTop <= 400) {            alpha = 1 - (document.body.scrollTop / 700);            header.style.background = "rgba(0,0,0," + alpha + ")";        }    })});