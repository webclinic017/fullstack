;
(function($,_) {
    'use strict';
    /*waiting to add plugins code*/
    /*用于写自己的插件*/
    /*移动端tap事件*/
    //$.fn._ = function(fn) {
    //    var o = this;
    //    o.isMove = false;
    //    o.on('touchstart', function(e) {
    //        o.startTime = +new Date();
    //    });
    //    o.on('touchmove', function(e) {
    //        o.isMove = true;
    //    });
    //    o.on('touchend', function(e) {
    //        o.endTime = +new Date();
    //        if ((o.endTime - o.startTime) <= 200 && !o.isMove) {
    //            fn && fn(e);
    //        }
    //        o.isMove = false;
    //    });
    //    return o;
    //}
}($,'tap'));